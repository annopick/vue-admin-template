# create-element-plus-admin

> **Status:** placeholder package. The CLI implementation arrives in slice 6 (issue #7).

This package will become the `npm create element-plus-admin` scaffolding tool. It embeds a snapshot of `packages/template` and generates a new project via:

```
npm create element-plus-admin my-app
```

See `docs/adr/0001-monorepo-and-cli-source.md` for the architecture decision.
