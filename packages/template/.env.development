# Base API. Empty in dev so requests hit the vite-plugin-mock server directly
# (mocks register at /user/login, not /dev-api/user/login). Production sets a
# real prefix that the reverse proxy strips.
VITE_APP_BASE_API=
